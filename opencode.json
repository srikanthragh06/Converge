{
    "$schema": "https://opencode.ai/config.json",
    "permission": {
        "edit": "ask",
        "bash": "ask"
    },
    "command": {
        "toggle-permissions": {
            "template": "Read the opencode.json file in the project root. Toggle the permission values: if edit is 'ask', change both edit and bash to 'allow'. If edit is 'allow', change both to 'ask'. Write the updated file and confirm the new state.",
            "description": "Toggle permissions between ask and allow"
        }
    }
}
