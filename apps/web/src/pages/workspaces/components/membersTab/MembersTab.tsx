/**
 * Members tab (placeholder). Will list workspace members and allow
 * role management in a future iteration.
 */
const MembersTab = ({ workspaceId }: { workspaceId: number }) => {
    return (
        <div className="flex flex-col items-center justify-center h-full text-sm opacity-40">
            Members — coming soon
        </div>
    );
};

export default MembersTab;
