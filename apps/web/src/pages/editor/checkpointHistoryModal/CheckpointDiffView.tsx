/**
 * Right-side panel of CheckpointHistoryModal. Placeholder — will render the
 * diff for the currently selected checkpoint once built.
 */
const CheckpointDiffView = () => {
    return <div> Select a checkpoint to view its diff.</div>;
};

export default CheckpointDiffView;
